export const metadata = {
  title: "FiscalGem Contact",
  description: "FiscalGem Contact",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="">{children}</div>;
}
